--开源脚本欢迎使用
local Players = game:GetService("Players")
local Player = Players.LocalPlayer
local TweenService = game:GetService("TweenService")

if game.CoreGui:FindFirstChild("SimpleLoader") then return end

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "SimpleLoader"
ScreenGui.ResetOnSpawn = false
ScreenGui.Parent = game.CoreGui

local MainFrame = Instance.new("Frame")
MainFrame.Size = UDim2.new(0, 380, 0, 240)
MainFrame.Position = UDim2.new(0, -400, 0.5, -120)
MainFrame.BackgroundColor3 = Color3.fromRGB(0, 0, 20)
MainFrame.Active = false
MainFrame.Draggable = false
MainFrame.Parent = ScreenGui

local MainCorner = Instance.new("UICorner")
MainCorner.CornerRadius = UDim.new(0, 10)
MainCorner.Parent = MainFrame

local MainStroke = Instance.new("UIStroke")
MainStroke.Thickness = 2
MainStroke.Color = Color3.fromRGB(0, 100, 255)
MainStroke.Parent = MainFrame

TweenService:Create(
    MainFrame,
    TweenInfo.new(1.2, Enum.EasingStyle.Quart, Enum.EasingDirection.Out),
    {Position = UDim2.new(0.5, -190, 0.5, -120)}
):Play()

local CloseBtn = Instance.new("TextButton")
CloseBtn.Size = UDim2.new(0, 36, 0, 36)
CloseBtn.Position = UDim2.new(1, -42, 0, 6)
CloseBtn.Text = "X"
CloseBtn.TextColor3 = Color3.new(1, 1, 1)
CloseBtn.Font = Enum.Font.GothamBold
CloseBtn.TextSize = 18
CloseBtn.BackgroundColor3 = Color3.fromRGB(180, 30, 30)
CloseBtn.Parent = MainFrame

local CloseCorner = Instance.new("UICorner")
CloseCorner.CornerRadius = UDim.new(1, 0)
CloseCorner.Parent = CloseBtn

CloseBtn.MouseButton1Click:Connect(function()
    ScreenGui:Destroy()
end)

local TabBar = Instance.new("Frame")
TabBar.Size = UDim2.new(0, 90, 1, -40)
TabBar.Position = UDim2.new(0, 8, 0, 36)
TabBar.BackgroundColor3 = Color3.fromRGB(0, 0, 40)
TabBar.Parent = MainFrame

local TabCorner = Instance.new("UICorner")
TabCorner.CornerRadius = UDim.new(0, 8)
TabCorner.Parent = TabBar

local TabList = Instance.new("UIListLayout")
TabList.SortOrder = Enum.SortOrder.LayoutOrder
TabList.Padding = UDim.new(0, 6)
TabList.Parent = TabBar

local TabPadding = Instance.new("UIPadding")
TabPadding.PaddingTop = UDim.new(0, 8)
TabPadding.PaddingLeft = UDim.new(0, 4)
TabPadding.PaddingRight = UDim.new(0, 4)
TabPadding.Parent = TabBar

local LoadTab = Instance.new("TextButton")
LoadTab.Size = UDim2.new(1, 0, 0, 30)
LoadTab.Text = "加载"
LoadTab.TextColor3 = Color3.fromRGB(255, 255, 255)
LoadTab.Font = Enum.Font.GothamBold
LoadTab.TextSize = 11
LoadTab.BackgroundColor3 = Color3.fromRGB(0, 20, 80)
LoadTab.LayoutOrder = 0
LoadTab.Parent = TabBar

local LoadTabCorner = Instance.new("UICorner")
LoadTabCorner.CornerRadius = UDim.new(0, 6)
LoadTabCorner.Parent = LoadTab

local ContentFrame = Instance.new("Frame")
ContentFrame.Size = UDim2.new(1, -105, 1, -40)
ContentFrame.Position = UDim2.new(0, 100, 0, 36)
ContentFrame.BackgroundTransparency = 1
ContentFrame.Parent = MainFrame

local LoadContent = Instance.new("ScrollingFrame")
LoadContent.Size = UDim2.new(1, 0, 1, 0)
LoadContent.BackgroundTransparency = 1
LoadContent.ScrollBarThickness = 4
LoadContent.ScrollingDirection = Enum.ScrollingDirection.Y
LoadContent.AutomaticCanvasSize = Enum.AutomaticSize.Y 
LoadContent.CanvasSize = UDim2.new(0, 0, 0, 0)         
LoadContent.Parent = ContentFrame

local ButtonList = Instance.new("UIListLayout")
ButtonList.SortOrder = Enum.SortOrder.LayoutOrder
ButtonList.Padding = UDim.new(0, 6)
ButtonList.Parent = LoadContent

local ButtonPadding = Instance.new("UIPadding")
ButtonPadding.PaddingTop = UDim.new(0, 10)
ButtonPadding.PaddingLeft = UDim.new(0, 5)
ButtonPadding.PaddingRight = UDim.new(0, 5)
ButtonPadding.Parent = LoadContent

local function CreateButton(text, order, callback)
    local ButtonFrame = Instance.new("Frame")
    ButtonFrame.Size = UDim2.new(1, 0, 0, 36)
    ButtonFrame.BackgroundColor3 = Color3.fromRGB(0, 0, 45)
    ButtonFrame.LayoutOrder = order
    ButtonFrame.Parent = LoadContent

    local Corner = Instance.new("UICorner")
    Corner.CornerRadius = UDim.new(0, 6)
    Corner.Parent = ButtonFrame

    local Button = Instance.new("TextButton")
    Button.Size = UDim2.new(1, 0, 1, 0)
    Button.Text = text
    Button.TextColor3 = Color3.fromRGB(255, 255, 255)
    Button.Font = Enum.Font.GothamBold
    Button.TextSize = 11
    Button.BackgroundTransparency = 1
    Button.Parent = ButtonFrame

    Button.MouseButton1Click:Connect(callback)
end

CreateButton("僵尸竞技场", 1, function()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/TypingSP/Capsule/main/%E5%83%B5%E5%B0%B8%E7%AB%9E%E6%8A%80%E5%9C%BA"))()
end)

CreateButton("刀刃球", 2, function()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/TypingSP/Capsule/main/%E5%88%80%E5%88%83%E7%90%83"))()
end)

CreateButton("后街生存", 3, function()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/TypingSP/Capsule/main/%E5%90%8E%E8%A1%97%E7%94%9F%E5%AD%98"))()
end)

CreateButton("重型钓鱼", 4, function()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/TypingSP/Capsule/main/%E9%87%8D%E5%9E%8B%E9%92%93%E9%B1%BC"))()
end)
